# ══════════════════════════════════════════════════════════════
# Cloudflare Pages — Redirects
# Optimizado para SEO + SPA routing
# ══════════════════════════════════════════════════════════════

# ──────────────────────────────────────────────────────────────
# Forzar HTTPS y dominio canónico
# ──────────────────────────────────────────────────────────────

http://metasoft.pe/* https://www.metasoft.pe/:splat 301
http://www.metasoft.pe/* https://www.metasoft.pe/:splat 301
https://metasoft.pe/* https://www.metasoft.pe/:splat 301

# ──────────────────────────────────────────────────────────────
# SPA fallback (Vue Router)
# ──────────────────────────────────────────────────────────────
/* /index.html 200